<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- TV Features -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- TV Intent Filter -->
    <uses-feature
        android:name="android.hardware.type.television"
        android:required="false" />

    <application
        android:name=".TVContentViewerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVContentViewer"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TVContentViewer.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.ScreenDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.TVContentViewer.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />
            
        <activity
            android:name=".ui.MediaPlayerActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScreenDetailActivity" />

        <activity
            android:name=".ui.AndroidPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.TVContentViewer.NoActionBar"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.TVPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.TVContentViewer.NoActionBar"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:hardwareAccelerated="true"
            android:largeHeap="true" />

    </application>

</manifest>
